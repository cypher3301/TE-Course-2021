package hw;


/**
 * Interface state pattern
 * Every strategy of university person have a state
 */
public interface WorkState {

    /**
     * Saving a work state
     */
    void save(Object o);
}
